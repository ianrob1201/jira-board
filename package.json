{
  "name": "jira-board",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "firebase": "^4.10.1",
    "jira-connector": "2.7.0",
    "query-string": "^6.1.0",
    "react": "^16.3.1",
    "react-dom": "^16.3.1",
    "react-router": "^4.2.0",
    "react-router-dom": "^4.3.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "docker": "docker run --rm -v $(pwd):/src -w /src -p 3000:3000 node:6 bash -c \"npm install && npm start\""
  }
}
